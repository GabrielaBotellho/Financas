# Copie este arquivo para .env (uso local) OU configure estas duas
# variáveis diretamente no painel da Vercel (Settings > Environment Variables).
# NUNCA cometa o arquivo .env de verdade no Git.

PLUGGY_CLIENT_ID=
PLUGGY_CLIENT_SECRET=
